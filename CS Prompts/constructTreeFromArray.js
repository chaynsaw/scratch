function Tree(val) {
  this.val = val;
  this.left = null;
  this.right = null;
}
let tree = new Tree(3);
let arr = [3,6,2,9,-1,10]
function arrayToTree(arr) {
  let newTree = new Tree(arr[0]);
  for (let i = 1; i < arr.length; i++) {
    if (newTree.left == null) {

    }
  }
}

Tree.prototype.setNode = function() {
  
}

